services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3243:3243"
    volumes:
      - ./conf:/usr/src/app/conf
      - ./data:/usr/src/app/data
    environment:
      - NODE_ENV=production
    command: npm start
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
